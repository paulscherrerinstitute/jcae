package ch.psi.jcae.impl.type;

/**
 * Marker interface to identify complex types that contain an array value (e.g.,
 * {@link IntegerArrayTimestamp}.
 */
public interface ArrayValueHolder {
	// nop
}
